# Ports (10400+)
POSTGRES_PORT=10400
API_PORT=10401
FRONTEND_PORT=10402
REDIS_PORT=10403

# PostgreSQL (for local: host=localhost, port=10400; docker overrides host=postgres, port=5432)
POSTGRES_USER=user
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=10400
POSTGRES_DB=dating_db

# JWT (generate: openssl rand -hex 32)
JWT_SECRET=
JWT_ACCESS_EXPIRE_SECONDS=86400
JWT_REFRESH_EXPIRE_SECONDS=604800

# Superadmin (12+ chars)
SUPERADMIN_LOGIN=admin
SUPERADMIN_PASSWORD=

# Telegram Bot (@BotFather)
TELEGRAM_BOT_TOKEN=

# API
API_URL=http://localhost:10401

# Bot API (for admin support responses; docker: http://bot:8002)
BOT_API_URL=http://localhost:8002
API_SERVER_PORT=8002

# YooKassa
YOOKASSA_SHOP_ID=
YOOKASSA_API_KEY=

# Redis (optional, for file_id cache + feed cache)
REDIS_URL=redis://localhost:10403/0

# i18n
DEFAULT_LOCALE=ru_RU
SUPPORTED_LOCALES=ru_RU,en_US

# Expiration service (premium expiry check)
EXPIRATION_CHECK_INTERVAL_SEC=3600

# Frontend (Vite)
VITE_API_URL=http://localhost:10401
VITE_DEFAULT_LOCALE=ru_RU
VITE_SUPPORTED_LOCALES=ru_RU,en_US
